(function () {
    
    const nombre = 'Tomas';
    const apellido = 'Lodola';
    const edad = 30;

    /* const salida = nombre + ' ' + apellido + ' (' + edad + ')'; */
    const salida = `${nombre} \n ${apellido} \n ( ${edad + 100} )`;


    console.log(salida);

})();
